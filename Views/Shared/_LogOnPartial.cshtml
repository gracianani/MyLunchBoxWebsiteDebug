<div id="welcomeInfo">
<a href="/Mobile/">View Mobile Version</a> 
@if(Request.IsAuthenticated) {
    <text>
    Welcome <strong>@User.Identity.Name</strong>! 
    @Html.ActionLink("Log Off", "LogOff", "Account") 
    </text> 
}
else {
    @: @Html.ActionLink("Log In", "LogOn", "Account") 
}
</div>