<div id="welcomeInfo">
@Html.ActionLink("View Mobile Site", "ViewMobileSite", "Home", new { }, new {  })

@if(Request.IsAuthenticated) {
    <text>
    Hi, <strong>@User.Identity.Name</strong>! 
    @Html.ActionLink("Log Off", "LogOff", "Account") 
    </text> 
}
else {
    @: @Html.ActionLink("Log In", "LogOn", "Account") 
}
</div>